# Backend API base URL
NEXT_PUBLIC_API_URL=http://localhost:5000

# Web Push — must match the server's VAPID_PUBLIC_KEY exactly.
# Generate the pair server-side with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key_here
